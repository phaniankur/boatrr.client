/* eslint-disable import/prefer-default-export */
import { breakpoints } from "./breakpoints";

 const theme = {
  breakpoints,
  colors: {
    primeYellow: '#FFCC00',
    grey: '#',
    fadedBlue: "#8ED0E0",
  },
};

export default theme;
